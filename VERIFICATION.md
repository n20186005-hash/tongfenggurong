# تقرير التحقق

## فحوصات المصدر التي يمكن تنفيذها دون اتصال خارجي

- بنية المشروع أحادية الحزمة ولا تحتوي `pnpm-workspace.yaml`.
- النطاق النهائي موجود كإعداد وحيد في `astro.config.mjs` ويمكن أن يبقى فارغاً.
- sitemap مشروط بوجود `site` ولا توجد قائمة sitemap مكتوبة يدوياً.
- لا توجد قيم `lastmod` في المصدر.
- لا توجد نطاقات تجريبية أو عناوين تطوير محلية أو مخططات روابط خاصة بإضافات المتصفح في ملفات الموقع.
- iframe الخاص بـ Google Maps مضبوط على `ar` / `eg`.
- GA4 لا يتم تحميله قبل موافقة المستخدم.

## ملاحظة بيئة إنشاء الحزمة

بيئة الإنشاء الحالية تمنع أي اتصال شبكي من أدوات النظام، بما في ذلك npm/Wikimedia. لذلك لا يمكن من داخل هذه البيئة تنزيل pnpm/حزم Astro أو الصور الأصلية، ولا يمكن تنفيذ دورة `pnpm install --frozen-lockfile → pnpm check → pnpm build` فعلياً هنا. لا ينبغي اعتبار هذا الملف بديلاً عن فحص CI الحقيقي؛ عند تشغيل المشروع في بيئة متصلة يجب تنفيذ الأوامر الواردة في README وإصلاح أي خطأ قبل النشر.
